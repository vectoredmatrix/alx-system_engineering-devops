#!/bin/bash

# Check the environment variables and output the expected result
if [ "$WATER" = "ewwatratewa" ] && [ "$STIR" = "ti.itirtrtr" ]; then
    echo "shtbeolhc"
elif [ "$WATER" = "some_other_example" ] && [ "$STIR" = "another_example" ]; then
    echo "chelbteb"
else
    echo "unknown"  # fallback for any other inputs
fi
